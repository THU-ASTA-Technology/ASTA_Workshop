import React from "react";
import "./Blog.css";
const Blog = (props) => {
    //TODO: add blog logic, also remember to update Tape and Broadcast so that they can adapt to User
    return (
        <div>
            <h1>Your code here</h1>
        </div>
        
    );
}

export default Blog;